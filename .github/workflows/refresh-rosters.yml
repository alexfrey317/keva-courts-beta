name: Refresh Roster Snapshot

on:
  schedule:
    - cron: '17 * * * *'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: roster-sync
  cancel-in-progress: true

jobs:
  refresh:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Fetch latest roster snapshot
        id: fetch
        run: |
          set -euo pipefail
          mkdir -p public
          curl -fsSL "https://keva-push.alexfrey317.workers.dev/rosters" -o /tmp/rosters.json
          jq -e '.teams and (.teams | type == "object") and (.teams | length > 0)' /tmp/rosters.json > /dev/null
          populated_count="$(jq '[.teams[] | select(((.players // []) | length) > 0)] | length' /tmp/rosters.json)"
          if [ "$populated_count" -lt 100 ]; then
            echo "::warning::Skipping roster refresh because only $populated_count teams include players."
            echo "valid=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if [ -f public/rosters.json ]; then
            jq -s '{
              syncedAt: .[1].syncedAt,
              configured: .[1].configured,
              stale: .[1].stale,
              teams: ((.[0].teams // {}) + (.[1].teams // {}))
            } | with_entries(select(.value != null))' public/rosters.json /tmp/rosters.json > /tmp/rosters-merged.json
            mv /tmp/rosters-merged.json /tmp/rosters.json
          fi
          echo "valid=true" >> "$GITHUB_OUTPUT"

      - name: Update tracked snapshot
        if: steps.fetch.outputs.valid == 'true'
        id: snapshot
        run: |
          set -euo pipefail
          if [ -f public/rosters.json ] && cmp -s /tmp/rosters.json public/rosters.json; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          mv /tmp/rosters.json public/rosters.json
          echo "changed=true" >> "$GITHUB_OUTPUT"

      - name: Commit snapshot refresh
        if: steps.snapshot.outputs.changed == 'true'
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/rosters.json
          git commit -m "chore: refresh roster snapshot"
          git push

      - uses: actions/setup-node@v5
        if: steps.snapshot.outputs.changed == 'true'
        with:
          node-version: 20
          cache: npm

      - name: Build Pages artifact
        if: steps.snapshot.outputs.changed == 'true'
        run: npm ci && npm run build
        env:
          VITE_BASE_PATH: /${{ github.event.repository.name }}/

      - uses: actions/upload-pages-artifact@v4
        if: steps.snapshot.outputs.changed == 'true'
        with:
          path: dist

      - name: Deploy roster snapshot to Pages
        if: steps.snapshot.outputs.changed == 'true'
        id: deployment
        uses: actions/deploy-pages@v5
